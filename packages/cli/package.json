{
  "name": "create-src20",
  "version": "0.1.0",
  "description": "Create an SRC20 token on Seismic in one command",
  "bin": {
    "create-src20": "dist/bin.js"
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "bun build src/bin.ts --outfile dist/bin.js --target node --external seismic-viem --external viem --external prompts --external picocolors --external yargs",
    "dev": "bun build src/bin.ts --outfile dist/bin.js --target node --external seismic-viem --external viem --external prompts --external picocolors --external yargs --watch"
  },
  "dependencies": {
    "seismic-viem": "^1.1.2",
    "viem": "^2.23.2",
    "prompts": "^2.4.2",
    "picocolors": "^1.1.1",
    "yargs": "^17.7.2"
  },
  "devDependencies": {
    "@types/prompts": "^2.4.9",
    "@types/yargs": "^17.0.33",
    "typescript": "^5.7.0"
  },
  "license": "AGPL-3.0"
}
